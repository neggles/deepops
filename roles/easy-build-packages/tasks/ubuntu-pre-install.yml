#
# install software modules using EasyBuild:
#
---
- name: "install depending software"
  become: true
  ansible.builtin.apt:
    name:
      - libssl-dev
      - build-essential
      - libsysfs-dev
      - libibverbs-dev
