---
- name: Install docker
  import_playbook: docker.yml

- name: Install nginx docker registry cache server
  hosts: "{{ hostlist | default('all') }}"
  become: true
  tasks:
    - name: Deploy nginx container caching proxy
      include_role:
        name: "nginx-docker-registry-cache"
        tasks_from: server
