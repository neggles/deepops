---
- name: Install docker
  import_playbook: ../container/docker.yml

- name: Configure Alertmanager
  hosts: "{{ hostlist | default('all') }}"
  become: true
  tasks:
    - name: Configure alertmanager
      include_role:
        name: alertmanager
